# JavaScript Notes

There is two types of writing functions in objects: method syntax and function syntax.

In method syntax, we don't use the `function`keyword.

In function syntax, we use the`function` keyword.`,
