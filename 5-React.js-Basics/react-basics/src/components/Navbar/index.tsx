import "./index.scss";

// type navList = {
//   navList: string[];
// };

const Navbar = ({ navList }: { navList: string[] }) => {
  console.log(navList);
  return (
    <>
      <nav>10</nav>
    </>
  );
};

export default Navbar;
